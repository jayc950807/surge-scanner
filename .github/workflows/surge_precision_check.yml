name: Surge Precision Check

on:
  workflow_dispatch:
    inputs:
      sample_size:
        description: "탐색 종목 수 (10000 = 전수)"
        required: false
        default: "10000"

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install yfinance pandas numpy lxml html5lib

      - name: Run precision check
        env:
          SAMPLE_SIZE: ${{ github.event.inputs.sample_size }}
        run: python research/surge_precision_check.py

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: surge-precision-results
          path: data/

      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          git diff --staged --quiet || git commit -m "precision check ($(date +%Y-%m-%d))"
          git push

      - name: Step Summary
        if: always()
        run: |
          if [ -f data/precision_report.md ]; then
            cat data/precision_report.md >> $GITHUB_STEP_SUMMARY
          fi
